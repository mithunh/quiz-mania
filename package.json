{
  "name": "quiz-app",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "start": "concurrently \"tailwindcss -i ./src/input.css -o ./dist/output.css --watch\" \"live-server --port=5500 --watch=dist,.\""
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "concurrently": "^7.6.0",
    "live-server": "^1.2.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.0.0"
  }
}
